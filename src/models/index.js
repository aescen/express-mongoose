const InstructorsModel = require('./intructors');
const CoursesModel = require('./courses');
const ParticipantsModel = require('./participants');

module.exports = {
  InstructorsModel,
  CoursesModel,
  ParticipantsModel,
};
